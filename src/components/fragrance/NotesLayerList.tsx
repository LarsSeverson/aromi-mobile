import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const NotesLayerList = () => {
  return (
    <View>
      <Text>NotesLayerList</Text>
    </View>
  )
}

export default NotesLayerList

const styles = StyleSheet.create({})
